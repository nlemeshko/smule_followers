apiVersion: v1
kind: Service
metadata:
  name: {{ include "smule-followers.fullname" . }}
  labels:
    {{- include "smule-followers.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "smule-followers.selectorLabels" . | nindent 4 }}
